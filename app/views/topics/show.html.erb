<h1> Topic </h1>

<table>
  <thead>
    <th>Name</th>
  </thead>
  <tr>
    <td><%= @topic.name%></td>
  </tr>
</table>

<%= link_to 'New Question', new_topic_question_path(@topic.id), class: 'btn btn-sm btn-success' %>